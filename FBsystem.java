import java.io.*;
import java.util.*;

public class FBsystem {
    // id and user
    public static HashMap<Integer, user> users = new HashMap<Integer, user>();
    public static HashMap<Long, Conversation> conversations = new HashMap<Long, Conversation>();
    public static HashMap<Integer, Post> posts = new HashMap<Integer, Post>();
    public static HashMap<Integer, Reply> Replies = new HashMap<Integer, Reply>();
    public static HashMap<Integer, comment> Comments = new HashMap<Integer, comment>();
}
